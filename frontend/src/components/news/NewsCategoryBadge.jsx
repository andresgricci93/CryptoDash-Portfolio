const NewsCategoryBadge = ({ children }) => (
  <span className="text-[11px] font-semibold uppercase tracking-[0.14em] text-white transition-[text-shadow] duration-200 hover:[text-shadow:0_0_10px_rgba(255,255,255,0.8)]">
    {children}
  </span>
);

export default NewsCategoryBadge;
